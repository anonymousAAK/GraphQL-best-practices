type Query {
  products: [Product]
  productsByPrice(minPrice: Float,maxPrice): [Product]
  productById(id: ID!): Product
}

type Product {
  id: ID!
  description: String!
  review: [Review]
  price: Float!
}
type Review {
  comment: String
  rating: Int!
}
